DATABASE_URL=postgres://navx:navx@localhost:5432/navx?sslmode=disable
PORT=8080
REDIS_URL=redis://localhost:6379
# Dev-only placeholder — generate a real random secret and keep it out of
# version control for any non-local deployment.
JWT_SECRET=dev-only-insecure-secret-change-me
